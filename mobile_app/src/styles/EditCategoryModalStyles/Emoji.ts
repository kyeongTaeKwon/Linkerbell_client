import styled from "styled-components/native";
// type Props = {
//   OS?: string;
// };

export const Emoji = styled.Text`
  font-family: "NMedium";
  font-size: 19px;
  padding-bottom: 0px;
  text-align: center;
  line-height: ${19 * 1.6}px;
  color: #000;
`;

// margin-top: ${(props: Props) => (props.OS === "ios" ? "140px" : "80px")};
