import styled from "styled-components/native";

type Props = {
  width: number;
};

export const HashTag = styled.Text`
  font-family: "NBold";
  font-size: 17px;
  align-self: center;
  margin-left: 8px;
  line-height: ${17 * 1.6}px;
`;
